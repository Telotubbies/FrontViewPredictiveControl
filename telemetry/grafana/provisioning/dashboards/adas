{
  "annotations": { "list": [] },
  "editable": true,
  "title": "ADAS Full Suite Dashboard",
  "tags": ["adas", "carla"],
  "timezone": "browser",
  "panels": [
    {
      "type": "gauge",
      "title": "Vehicle Speed (km/h)",
      "gridPos": { "h": 6, "w": 4, "x": 0, "y": 0 },
      "fieldConfig": {
        "defaults": { "min": 0, "max": 60, "unit": "km/h" },
        "overrides": []
      },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "vehicle" and r._field == "speed_kmh") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "gauge",
      "title": "Steering",
      "gridPos": { "h": 6, "w": 4, "x": 4, "y": 0 },
      "fieldConfig": {
        "defaults": { "min": -1, "max": 1, "unit": "none" },
        "overrides": []
      },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "vehicle" and r._field == "steering") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "gauge",
      "title": "Throttle",
      "gridPos": { "h": 6, "w": 4, "x": 8, "y": 0 },
      "fieldConfig": {
        "defaults": { "min": 0, "max": 1, "unit": "none" },
        "overrides": []
      },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "vehicle" and r._field == "throttle") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "gauge",
      "title": "Brake",
      "gridPos": { "h": 6, "w": 4, "x": 12, "y": 0 },
      "fieldConfig": {
        "defaults": { "min": 0, "max": 1, "unit": "none" },
        "overrides": []
      },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "vehicle" and r._field == "brake") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "stat",
      "title": "AEB Status",
      "gridPos": { "h": 3, "w": 4, "x": 16, "y": 0 },
      "fieldConfig": {
        "defaults": { "color": { "mode": "thresholds" },
          "thresholds": { "steps": [
            { "color": "green", "value": null },
            { "color": "red", "value": 1 }
          ]}
        },
        "overrides": []
      },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "aeb" and r._field == "active") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "stat",
      "title": "AEB TTC (s)",
      "gridPos": { "h": 3, "w": 4, "x": 16, "y": 3 },
      "fieldConfig": {
        "defaults": { "unit": "s", "color": { "mode": "thresholds" },
          "thresholds": { "steps": [
            { "color": "green", "value": null },
            { "color": "yellow", "value": 2.5 },
            { "color": "orange", "value": 1.5 },
            { "color": "red", "value": 1 }
          ]}
        },
        "overrides": []
      },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "aeb" and r._field == "ttc") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "timeseries",
      "title": "Speed & Target Speed (m/s)",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 6 },
      "fieldConfig": { "defaults": { "unit": "m/s" }, "overrides": [] },
      "targets": [
        {
          "query": 'from(bucket: "adas_telemetry") |> range(start: -5m) |> filter(fn: (r) => r._measurement == "vehicle" and r._field == "speed_ms")',
          "refId": "A", "legendFormat": "ego_speed"
        },
        {
          "query": 'from(bucket: "adas_telemetry") |> range(start: -5m) |> filter(fn: (r) => r._measurement == "acc" and r._field == "target_speed_ms")',
          "refId": "B", "legendFormat": "acc_target"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "CTE & Heading Error",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 6 },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "targets": [
        {
          "query": 'from(bucket: "adas_telemetry") |> range(start: -5m) |> filter(fn: (r) => r._measurement == "lane" and r._field == "cte_m")',
          "refId": "A", "legendFormat": "CTE (m)"
        },
        {
          "query": 'from(bucket: "adas_telemetry") |> range(start: -5m) |> filter(fn: (r) => r._measurement == "lane" and r._field == "heading_err_rad")',
          "refId": "B", "legendFormat": "Heading err (rad)"
        }
      ]
    },
    {
      "type": "stat",
      "title": "LDW State",
      "gridPos": { "h": 3, "w": 3, "x": 0, "y": 14 },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "ldw" and r._field == "state_str") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "stat",
      "title": "BSW Left",
      "gridPos": { "h": 3, "w": 3, "x": 3, "y": 14 },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "bsw" and r._field == "left_alert_str") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "stat",
      "title": "BSW Right",
      "gridPos": { "h": 3, "w": 3, "x": 6, "y": 14 },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "bsw" and r._field == "right_alert_str") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "stat",
      "title": "Traffic Light",
      "gridPos": { "h": 3, "w": 3, "x": 9, "y": 14 },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "tsr" and r._field == "traffic_light_str") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "stat",
      "title": "TJA State",
      "gridPos": { "h": 3, "w": 3, "x": 12, "y": 14 },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "tja" and r._field == "state_str") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "stat",
      "title": "Speed Limit (km/h)",
      "gridPos": { "h": 3, "w": 3, "x": 15, "y": 14 },
      "fieldConfig": { "defaults": { "unit": "km/h" }, "overrides": [] },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -1m) |> filter(fn: (r) => r._measurement == "tsr" and r._field == "speed_limit_kmh") |> last()',
        "refId": "A"
      }]
    },
    {
      "type": "timeseries",
      "title": "ACC Distance to Lead (m)",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 17 },
      "fieldConfig": { "defaults": { "unit": "m" }, "overrides": [] },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -5m) |> filter(fn: (r) => r._measurement == "acc" and r._field == "distance_m")',
        "refId": "A", "legendFormat": "distance"
      }]
    },
    {
      "type": "timeseries",
      "title": "LKA Pro Steering Assist",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 17 },
      "fieldConfig": { "defaults": { "unit": "rad" }, "overrides": [] },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -5m) |> filter(fn: (r) => r._measurement == "lka_pro" and r._field == "assist")',
        "refId": "A", "legendFormat": "steer_assist"
      }]
    },
    {
      "type": "timeseries",
      "title": "MPC Solve Time (ms)",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 25 },
      "fieldConfig": { "defaults": { "unit": "ms" }, "overrides": [] },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -5m) |> filter(fn: (r) => r._measurement == "mpc" and r._field == "solve_time_ms")',
        "refId": "A", "legendFormat": "solve_time"
      }]
    },
    {
      "type": "timeseries",
      "title": "Loop FPS",
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 25 },
      "fieldConfig": { "defaults": { "unit": "fps" }, "overrides": [] },
      "targets": [{
        "query": 'from(bucket: "adas_telemetry") |> range(start: -5m) |> filter(fn: (r) => r._measurement == "performance" and r._field == "fps")',
        "refId": "A", "legendFormat": "fps"
      }]
    }
  ],
  "time": { "from": "now-5m", "to": "now" },
  "refresh": "1s"
}
